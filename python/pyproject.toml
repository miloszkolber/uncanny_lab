[project]
name = "legacy-image-lab"
version = "0.1.0"
description = "Replaceable Python image-generation workers for Legacy Image Lab"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
test = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["legacy_lab"]
